# Next.js marketing site only — picked up by `next build` / Vercel.
# Portal on its own host: origin only, no /portal. Override in Vercel if this domain changes.
# For true same-host monorepo (/portal on the marketing domain), clear this value.
NEXT_PUBLIC_PORTAL_URL=https://na-juristsportal.vercel.app
