# Portal API (`law-firm-portal/backend`, default :4000)
VITE_API_URL=http://localhost:4000

# Production builds: copy to .env.production or edit committed .env.production (HTTPS API URL, no trailing slash).
