# Used by `vite build` (and Vercel) — no need to set VITE_API_URL in the Vercel UI if this file is committed.
# HTTPS origin of law-firm-portal/backend only, no trailing slash. Change to your real deployed API.
VITE_API_URL=https://api.your-domain.com
